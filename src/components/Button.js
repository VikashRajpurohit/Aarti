import React from 'react';
import AnimatedButton from './AnimatedButton';

// Use AnimatedButton for all button instances
export default function Button(props) {
  return <AnimatedButton {...props} />;
}
